//#include "DataManager.hpp"
#include "provider.hpp"

int main() {
/*    DataManager data("./test.db");
    Provider prov("testEmail");
    std::vector<Provider> listProv;
    /*
    data.connectDB();
    data.insertProvider(prov);
    data.getAllProvider(listProv);
    data.disConnectDB();

    for (unsigned int i = 0; i < listProv.size(); i++) {
        std::cout << listProv[i].getEmail() << std::endl;
    }
    listProv.clear();
    //
    data.connectDB();
    prov.setProviderID("testID1");
    data.updateProvider(prov);
    data.getAllProvider(listProv);
    data.disConnectDB();
    for (unsigned int i = 0; i < listProv.size(); i++) {
        std::cout << listProv[i].getEmail() << " - " << listProv[i].getProviderID() << std::endl;
    }
    listProv.clear();
*/
/*
    DataManager db("./test.db");
    db.connectDB();
    db.insertProvider("testID5","testEmail5");
    db.insertProvider("testID6","testEmail6");
    db.insertProvider("testID7","testEmail7");
    db.insertProvider("testID8","testEmail8");
    db.insertProvider("testID9","testEmail9");
    db.getAllProvider();
    db.disConnectDB();
*/
    return 0;
}
